{
  "name": "ticmsg-v2-signaling",
  "version": "2.0.0",
  "description": "TicMsg v2 - Lightweight WebRTC signaling server (no auth, no DB)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "ws": "^8.14.2"
  }
}
